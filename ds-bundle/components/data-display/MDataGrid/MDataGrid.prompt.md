MDataGrid from ws-uikit. Use via `window.WsUikit.MDataGrid` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<WsThemeProvider>` (full provider chain in README.md — components read theme/i18n from that context).

# MDataGrid Component

The `MDataGrid` component is an interactive data table that supports sorting, selection, and infinite scrolling.

## Features

- Column sorting
- Rows selection
- Rows filtering
- Infinite scrolling for large datasets
- Custom renderers for data display

## Usage

### Default Example
