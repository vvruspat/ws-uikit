MProgressBar from ws-uikit. Use via `window.WsUikit.MProgressBar` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<WsThemeProvider>` (full provider chain in README.md — components read theme/i18n from that context).

# MProgressBar

A linear progress indicator for determinate or indeterminate work.


## Variants


## Sizes


## Props
