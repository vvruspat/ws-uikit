MDaySelector from ws-uikit. Use via `window.WsUikit.MDaySelector` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<WsThemeProvider>` (full provider chain in README.md — components read theme/i18n from that context).

Variants (see `MDaySelector.html`): Default.

## Examples

```jsx
// Default
export const Default: Story = {
	args: {},

	argTypes: {},
};
```

### Default

```jsx
/* Default */ compose(S, "Default")
```
