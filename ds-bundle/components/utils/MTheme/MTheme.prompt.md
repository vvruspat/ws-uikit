MTheme from ws-uikit. Use via `window.WsUikit.MTheme` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<WsThemeProvider>` (full provider chain in README.md — components read theme/i18n from that context).

# Definition

A Theme is a component that returns parameters inside the html tag

## Usage

## Texts

Theme has the following properties:
