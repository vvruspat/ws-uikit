MDivider from ws-uikit. Use via `window.WsUikit.MDivider` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<WsThemeProvider>` (full provider chain in README.md — components read theme/i18n from that context).

Variants (see `MDivider.html`): Basic.

## Examples

```jsx
// Basic
export const Basic: Story = {
	args: {},

	argTypes: {},
};
```

### Basic

```jsx
/* Basic */ compose(S, "Basic")
```
