MCard from ws-uikit. Use via `window.WsUikit.MCard` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<WsThemeProvider>` (full provider chain in README.md — components read theme/i18n from that context).

# MCard

A content container with optional header, footer, shadow, dividers, and collapsible behavior. Corner radii are individually configurable.

---

## Variants

### Full Card (Header + Content + Footer)


### With Dividers


### Content Only


---

## States

### Collapsed


### No Padding


---

## Props
