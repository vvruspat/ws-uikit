MSpinner from ws-uikit. Use via `window.WsUikit.MSpinner` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<WsThemeProvider>` (full provider chain in README.md — components read theme/i18n from that context).

# MSpinner

A lightweight loading indicator. Supports four color modes and seven sizes.

---

## Overview

### Modes


### Sizes


---

## Modes

| Mode | Description |
|------|-------------|
| `base` | Neutral / grayscale |
| `primary` | Brand primary color |
| `secondary` | Secondary brand color |
| `tertiary` | Tertiary brand color |

## Sizes

`xs` · `s` · `m` · `l` · `xl` · `3xl` · `4xl`

---

## Playground


## Props
