MCaption from ws-uikit. Use via `window.WsUikit.MCaption` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<WsThemeProvider>` (full provider chain in README.md — components read theme/i18n from that context).

# Definition

Caption is a clickable interactive element that triggers a response.

You can place text and icons inside of a Caption.

Captions are often used for form submissions and to toggle elements into view.

## Usage

## Captions

Caption has the following properties:
