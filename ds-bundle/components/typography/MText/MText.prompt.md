MText from ws-uikit. Use via `window.WsUikit.MText` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<WsThemeProvider>` (full provider chain in README.md — components read theme/i18n from that context).

# Definition

Text is a clickable interactive element that triggers a response.

You can place text and icons inside of a Text.

Texts are often used for form submissions and to toggle elements into view.

## Usage

## Texts

Text has the following properties:
