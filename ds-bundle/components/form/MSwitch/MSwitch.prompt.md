MSwitch from ws-uikit. Use via `window.WsUikit.MSwitch` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<WsThemeProvider>` (full provider chain in README.md — components read theme/i18n from that context).

# MSwitch

A toggle switch for binary on/off choices. Supports two border styles (`round` and `rectangular`) and icon slots for active/inactive indicators.

---

## Overview

All border types and states at a glance.


---

## Border Types

| Type | Description |
|------|-------------|
| `round` | Pill-shaped corners (default) |
| `rectangular` | Squared corners |

### Round


### Rectangular


---

## States

### Disabled


---

## Props
