MRadio from ws-uikit. Use via `window.WsUikit.MRadio` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<WsThemeProvider>` (full provider chain in README.md — components read theme/i18n from that context).

# MRadio

A radio button for selecting a single option from a set. Supports regular, active (checked), and invalid statuses, plus a disabled state.

---

## Overview

All statuses at a glance.


---

## Statuses

| Status | Description |
|--------|-------------|
| `regular` | Default, unchecked |
| `active` | Selected — checked state |
| `invalid` | Error state — red accent |

### Regular


### Active


### Invalid


---

## States

### Disabled


---

## Props
