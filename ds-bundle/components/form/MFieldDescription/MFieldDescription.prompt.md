MFieldDescription from ws-uikit. Use via `window.WsUikit.MFieldDescription` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<WsThemeProvider>` (full provider chain in README.md — components read theme/i18n from that context).

# Definition

FieldError is a clickable interactive element that triggers a response.

You can place text and icons inside of a FieldError.

FieldErrors are often used for form submissions and to toggle elements into view.

## Usage

## FieldErrors

FieldError has the following properties:
