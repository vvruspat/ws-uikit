MCheckbox from ws-uikit. Use via `window.WsUikit.MCheckbox` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<WsThemeProvider>` (full provider chain in README.md — components read theme/i18n from that context).

# MCheckbox

A checkbox input for selecting one or more options from a list. Supports two validation statuses and checked/disabled states.

---

## Overview

All statuses and states at a glance.


---

## Statuses

| Status | Description |
|--------|-------------|
| `regular` | Default — no validation feedback |
| `invalid` | Error state — red accent |

### Regular


### Invalid


---

## States

### Disabled


---

## Props
