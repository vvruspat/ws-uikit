MLabel from ws-uikit. Use via `window.WsUikit.MLabel` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<WsThemeProvider>` (full provider chain in README.md — components read theme/i18n from that context).

# Definition

Label is a clickable interactive element that triggers a response.

You can place text and icons inside of a Label.

Labels are often used for form submissions and to toggle elements into view.

## Usage

## Labels

Label has the following properties:
