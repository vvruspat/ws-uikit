MSlider from ws-uikit. Use via `window.WsUikit.MSlider` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<WsThemeProvider>` (full provider chain in README.md — components read theme/i18n from that context).

# MSlider

A single-value slider for choosing a numeric value from a bounded range.


## Variants


## Disabled


## Props
