MFileInput from ws-uikit. Use via `window.WsUikit.MFileInput` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<WsThemeProvider>` (full provider chain in README.md — components read theme/i18n from that context).

# Definition

Input is a clickable interactive element that triggers a response.

You can place text and icons inside of a Input.

Inputs are often used for form submissions and to toggle elements into view.

## Usage

## Inputs

Input has the following properties:

### Regular


#### Available parameters


### Valid

### Invalid
### Disabled
