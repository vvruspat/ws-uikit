MListItem from ws-uikit. Use via `window.WsUikit.MListItem` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<WsThemeProvider>` (full provider chain in README.md — components read theme/i18n from that context).

# Definition

A list item can be used to display content related to a single subject.

Used inside the `<List/>` component

## Usage
