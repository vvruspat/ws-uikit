MTab from ws-uikit. Use via `window.WsUikit.MTab` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<WsThemeProvider>` (full provider chain in README.md — components read theme/i18n from that context).

# Definition

Tab for tabs

## Usage

## Tab

Tab has the following properties:
