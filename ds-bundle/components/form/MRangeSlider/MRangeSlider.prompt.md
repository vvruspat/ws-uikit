MRangeSlider from ws-uikit. Use via `window.WsUikit.MRangeSlider` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<WsThemeProvider>` (full provider chain in README.md — components read theme/i18n from that context).

# MRangeSlider

A two-thumb slider for choosing a numeric range.


## Variants


## Disabled


## Props
