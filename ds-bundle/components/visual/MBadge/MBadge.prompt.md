MBadge from ws-uikit. Use via `window.WsUikit.MBadge` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<WsThemeProvider>` (full provider chain in README.md — components read theme/i18n from that context).

# MBadge

A small label used to display status, category, or count information inline. Supports six semantic color modes.

---

## Overview

All modes at a glance.


---

## Modes

| Mode | Usage |
|------|-------|
| `primary` | Default, brand-colored label |
| `transparent` | Subtle, minimal background |
| `info` | Informational / neutral |
| `success` | Positive / completed |
| `warning` | Caution / attention needed |
| `error` | Destructive / failed |

### Primary


### Transparent


### Info


### Success


### Warning


### Error


---

## Props
