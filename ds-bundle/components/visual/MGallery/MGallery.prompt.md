MGallery from ws-uikit. Use via `window.WsUikit.MGallery` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<WsThemeProvider>` (full provider chain in README.md — components read theme/i18n from that context).

# Definition

Gallery is used to display any elements as slidable items.
