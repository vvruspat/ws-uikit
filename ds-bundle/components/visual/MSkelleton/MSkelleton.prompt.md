MSkelleton from ws-uikit. Use via `window.WsUikit.MSkelleton` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<WsThemeProvider>` (full provider chain in README.md — components read theme/i18n from that context).

# Definition

Skelleton is a visual representation of pendig state for data.

## Usage

## Skelleton

Flex has the following properties:
