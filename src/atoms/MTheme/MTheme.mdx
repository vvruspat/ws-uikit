import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';

import * as ThemeStories from './MTheme.stories';

<Meta of={ThemeStories} />

# Definition

A Theme is a component that returns parameters inside the html tag

## Usage

## Texts

Theme has the following properties:

<Canvas of={ThemeStories.Basic} />

<Controls />
