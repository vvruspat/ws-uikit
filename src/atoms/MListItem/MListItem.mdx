import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';

import * as ListItemStories from './MListItem.stories';

<Meta of={ListItemStories} />

# Definition

A list item can be used to display content related to a single subject.

Used inside the `<List/>` component

## Usage

<Canvas of={ListItemStories.Basic} />

---

## Customization

MListItem is styled with CSS variables. See **Guides/Theming & Customization** for the
override mechanism (global / zonal / instance). Below is the component's public token API.

**Geometry — component tokens** (override per-component; default falls to a semantic role):

| Token | Controls | Default |
|-------|----------|---------|
| `--list-item-padding-y` | Vertical padding | `--gap-s` |
| `--list-item-padding-x` | Horizontal padding | `--gap-l` |
| `--list-item-gap` | Gap between the before/content/after slots | `--gap-m` |

**Colors** have no per-component token by design — they bind to shared semantic roles so
MListItem stays in lock-step with the rest of the system.

| State | Semantic role | → primitive (default) |
|-------|----------------|-----------------------|
| hover / focused | `--color-surface-overlay` | `--palette-base-100` |
| active / pressed | `--color-surface-sunken` | `--palette-base-200` |
| divider | `--color-border-default` | `--palette-alpha-black-12` |
| focus ring | `--color-focus-ring` | `--palette-primary-700` |

Focus geometry is the shared `--focus-ring-width` / `--focus-ring-offset`.

> Recoloring for a whole brand is a global concern — see **Guides/Theming & Customization**.

<Controls />
