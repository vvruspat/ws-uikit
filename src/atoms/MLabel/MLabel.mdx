import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';

import * as LabelStories from './MLabel.stories';

<Meta of={LabelStories} />

# Definition

Label is a clickable interactive element that triggers a response.

You can place text and icons inside of a Label.

Labels are often used for form submissions and to toggle elements into view.

## Usage

## Labels

Label has the following properties:

<Canvas of={LabelStories.Basic} />

<Controls />
