import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';

import * as TextStories from './MText.stories';

<Meta of={TextStories} />

# Definition

Text is a clickable interactive element that triggers a response.

You can place text and icons inside of a Text.

Texts are often used for form submissions and to toggle elements into view.

## Usage

## Texts

Text has the following properties:

<Canvas of={TextStories.Basic} />

<Controls />
