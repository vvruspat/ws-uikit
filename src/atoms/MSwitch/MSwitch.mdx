import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';

import * as SwitchStories from './MSwitch.stories';

<Meta of={SwitchStories} />

# MSwitch

A toggle switch for binary on/off choices. Supports two border styles (`round` and `rectangular`) and icon slots for active/inactive indicators.

---

## Overview

All border types and states at a glance.

<Canvas of={SwitchStories.AllVariants} />

---

## Border Types

| Type | Description |
|------|-------------|
| `round` | Pill-shaped corners (default) |
| `rectangular` | Squared corners |

### Round

<Canvas of={SwitchStories.Regular} />

### Rectangular

<Canvas of={SwitchStories.Rectangular} />

---

## States

### Disabled

<Canvas of={SwitchStories.Disabled} />

---

## Props

<Controls />
