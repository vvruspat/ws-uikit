import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';

import * as FlexStories from './MFlex.stories';

<Meta of={FlexStories} />

# Definition

Flex is a clickable interactive element that triggers a response.

You can place text and icons inside of a Flex.

Flexs are often used for form submissions and to toggle elements into view.

## Usage

## Flex

Flex has the following properties:

<Canvas of={FlexStories.Basic} />

<Controls />
