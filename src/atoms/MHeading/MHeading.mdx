import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';

import * as HeadingStories from './MHeading.stories';

<Meta of={HeadingStories} />

# Definition

Heading is a clickable interactive element that triggers a response.

You can place heading and icons inside of a Heading.

Headings are often used for form submissions and to toggle elements into view.

## Usage

## Headings

Heading has the following properties:

<Canvas of={HeadingStories.Basic} />

<Controls />
