import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';

import * as AlertStories from './MAlert.stories';

<Meta of={AlertStories} />

# Definition

`MAlert` is a UI component designed to display alert messages to the user. These are typically used for conveying important information, such as:

- Success confirmations
- Warnings
- Error messages
- Informational notices

## Usage

## Alert

Flex has the following properties:

<Canvas of={AlertStories.Basic} />

<Controls />
