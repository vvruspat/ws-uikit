import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';

import * as AvatarStories from './MAvatar.stories';

<Meta of={AvatarStories} />

# Definition

Avatar is a visual representation of a user or entity.

You can display an image, initials, or an icon inside an Avatar.

Avatars are commonly used in profiles, chat interfaces, and user lists.

## Usage

## Avatar

Flex has the following properties:

<Canvas of={AvatarStories.Basic} />

<Controls />
